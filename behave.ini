[behave]
paths = features
show_skipped = false
show_timings = true
color = true
junit = true
junit_directory = reports/junit

[behave.userdata]
# Overridable at runtime, e.g. `behave -D headless=false -D browser=firefox`
headless = true
browser = chrome
